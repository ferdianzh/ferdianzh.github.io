

// ======================================
// ======================================
// ==========                  ==========
// ==========    NOT FINISH    ==========
// ==========                  ==========
// ======================================
// ======================================


const testItems = [
  {
    tag: 'div',
    classList: ['m-3', 'px-2'],
    children: [
      {
        tag: 'div',
        classList: ['m-3', 'px-2'],
        children: [
          {
            tag: 'h3',
            classList: ['text-2xl'],
          }
        ],
      },
    ],
  },
];

function renderComponent(item) {
  const items = item;
  let result;
}

const render = (item = item) => {
  let result;
  const mainContainer = item.map(it => {
  });
}